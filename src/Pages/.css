.metrics {
  flex: 1;
  background: rgba(15, 23, 42, 0.34);
  border: 1px solid rgba(168, 85, 247, 0.15);
  border-radius: 16px;
  padding: 16px 18px;
}

.metrics h3 {
  margin: 0 0 12px;
  color: #f2ebff;
}

.metric-row {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  padding: 10px 0;
  border-bottom: 1px dashed rgba(196, 181, 253, 0.25);
  color: #f4f1ff;
}

.metric-row:last-child {
  border-bottom: none;
}

.metric-label {
  color: #d3c6ff;
}

.metric-value {
  color: #f7f5ff;
  font-weight: 600;
}
